import styled from 'styled-components';

export const Wrapper = styled.main`
  position: relative;
  display: flex;

  height: 100vh;
  width: 100vw;

  overflow: hidden;

  .leaflet-container {
    background: #333;
  }
`;